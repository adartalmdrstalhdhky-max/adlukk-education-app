name: adlukk_app
description: ADLUKK Global Education Platform
publish_to: "none"
environment:
sdk: ">=2.17.0 <4.0.0"
dependencies:
flutter:
sdk: flutter
State Management
provider: ^6.1.1 # لإدارة حالة التطبيق
Local Database
isar: ^3.1.0+1 # قاعدة بيانات محلية عالية الأداء
isar_flutter_libs: ^3.1.0+1 # ملفات المكتبة الخاصة بالمنصات
dev_dependencies:
flutter_test:
sdk: flutter
Code Generation for Isar
isar_generator: ^3.1.0+1 # لتوليد كود نماذج البيانات
build_runner: ^2.4.7 # لتشغيل مُولّد الكود
flutter:
uses-material-design: true
assets:
- assets/
